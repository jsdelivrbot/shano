{% if slides %}
  <div{{ attributes.addClass('editorial-hero-slider', 'slider-container') }}>
    <div{{ content_attributes.addClass('inner-content') }}>
      {{ slides }}
    </div>
  </div>
{% endif %}
