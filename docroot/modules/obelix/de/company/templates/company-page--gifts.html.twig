<div class="container">
    {% for row in gifts|batch(3) %}
    <div class="has--editorial-content--relation-teaser container item-4">
        <div class="row">
            {% for gift in row %}
                <div class="col-xs-12 col-sm-4 col-md-4 col-lg-4">
                    {{ gift|raw }}
                </div>
            {% endfor %}
        </div>
    </div>
    {% endfor %}
</div>
