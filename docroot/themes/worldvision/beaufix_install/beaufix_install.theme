<?php
/**
 * @file
 * beaufix_install is a hidden theme just for the installation process.
 */

/**
 * Implements hook_preprocess_install_page().
 */
function beaufix_install_preprocess_install_page(&$variables) {
  // beaufix_install has custom styling for the install page.
  $variables['#attached']['library'][] = 'beaufix_install/install-page';
}
