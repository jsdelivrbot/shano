{#
/**
 * This template is used when viewing the wovi form section.
 *
 * Available variables:
 * - form: A list of all form elements.
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('container') }}>
    <div class="row">
        {% if elements.headline %}
            <div class="col-xs-12">
                <h2 class="form-hl text-center font-size-m">{{ elements.headline }}</h2>
            </div>
        {% endif %}
        {% if elements.subline %}
            <div class="col-xs-12">
                <p class="form-sl text-center font-size-s">{{ elements.subline }}</p>
            </div>
        {% endif %}
        {# Wrap smaller on large BP #}
        <div class="col-xs-12 col-sm-8 col-sm-offset-2 col-md-6 col-md-offset-3">
            <div class="row">
                <div class="col-xs-12">{{ elements.paymentMethod }}</div>
                <div class="clearfix"></div>
                <div class="wrapper-fields">
                    <div class="bank-fields">
                        <div class="col-xs-12 col-sm-6">{{ elements.IBAN }}</div>
                        <div class="col-xs-12 col-sm-6">{{ elements.swiftCode }}</div>
                        {# TODO When DE theme will be replaced with this one we need to make it visible depends on  country & settings #}
                        <div class="col-xs-12 vert-offset-top-1 hidden"><p class="text-center font-size-xs">Alternativ können Sie <br class="visible-xs"/><a
                                        class="link-underlined action-ktnr-blz"
                                        href="javascript:void(0);">hier Ihre Konto-Nr. / BLZ eingeben</a></p></div>
                        <div class="bank-ktnr-blz hidden">
                            <div class="col-xs-12 col-sm-6">{{ elements.bankAccountNo }}</div>
                            <div class="col-xs-12 col-sm-6">{{ elements.bankBranchNo }}</div>
                        </div>
                        <div class="clearfix"></div>
                    </div>
                    <div class="paypal-fields vert-offset-top-1">
                        {# TODO Make it translatable #}
                        <div class="col-xs-12"><p class="font-size-xs text-center-sm">Nach dem Absenden werden Sie automatisch an
                                PayPal
                                weitergeleitet.</p></div>
                    </div>
                    <div class="creditcard-fields vert-offset-top-1">
                        {# TODO Make it translatable #}
                        <div class="col-xs-12"><p class="font-size-xs text-center-sm">Nach dem Absenden werden Sie automatisch an
                                unser
                                Kreditkarten-System
                                weitergeleitet.</p></div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
