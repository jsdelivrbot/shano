{#
/**
 * This template is used when viewing the wovi form section.
 *
 * Available variables:
 * - form: A list of all form elements.
 *
 * @ingroup themeable
 */
#}
<div{{ attributes }}>
    <div class="container">
        <div class="row">
            {% if elements.headline %}
                <div class="col-xs-12">
                    <h2 class="form-hl text-center font-size-m">{{ elements.headline }}</h2>
                </div>
            {% endif %}
            {% if elements.subline %}
                <div class="col-xs-12">
                    <p class="form-sl text-center font-size-s">{{ elements.subline }}</p>
                </div>
            {% endif %}
            <div class="col-xs-12 col-sm-8 col-sm-offset-2 col-md-6 col-md-offset-3">
                <div class="col-xs-12">{{ elements.donationReceipt }}</div>
                <div class="col-xs-12">{{ elements.adressnumber }}</div>
            </div>

        </div>
    </div>
</div>
